ItemContainer = require('src.builtin.element')

-- Returns the container content mass (the sum of the mass of all the items it contains)
function ItemContainer.getItemsMass() 
end

-- Returns the container self mass
function ItemContainer.getSelfMass() 
end

return ItemContainer