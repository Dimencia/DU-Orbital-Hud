name: Wrap and Release
on:
  push:
    paths:
      - '*'
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    container: lua:latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -la
